<div class="container">
  <div class="row justify-content-center">
    <div class="col-10">
      <div class="card">
        <div class="card-body">

          <h2>検索結果</h2>
          <table class="table">
            <thead class="border-top">
              <tr>
                <th><%= "仕込み項目名" %></th>
              </tr>
            </thead>

            <tbody>
              <% @results.each do |food_prep| %>
                <tr>
                  <td><%= food_prep.name %></td>
                  <td><%= link_to t('詳細を見る'), food_prep_path(food_prep), method: :get, class: "btn btn-outline-primary btn-sm" %></td>
                  <td><%= link_to t('編集する'), edit_food_prep_path(food_prep), method: :get, class: "btn btn-outline-success btn-sm" %></td> 
                  <td><%= link_to t('削除する'), food_prep_path(food_prep), method: :delete, data: { confirm: '本当に削除しますか？' }, class: "btn btn-outline-danger btn-sm"  %></td> 
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>        
</div>
<br>
<%= link_to t('戻る'), food_preps_path(@results), class: "bi bi-skip-backward-fill btn btn-primary btn-sm"  %>

