<h1><%= t('.title') %></h1>

<table class = "table">
  <thead>
    <tr>
      <th><%= 'カテゴリ名' %></th>
      <th><%= '登録仕込み数' %></th>
    </tr>
  </thead>

  <tbody>
    <% @labels.each do |label| %>
      <tr>
        <td><%= label.name%></td>
        <td><%= label.food_preps.count %></td>
        <td><%= link_to t('common.edit'), edit_label_path(label) %></td>
        <td><%= link_to t('common.destroy'), label_path(label), method: :delete, data: { confirm: t('common.confirm_destroy') } %></td>
      </tr>
    <% end %>
  </tbody>
</table>