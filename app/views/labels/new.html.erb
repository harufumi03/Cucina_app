
<%= form_with(model: @label, local: true) do |form| %>
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-8">
        <div class="card">
          <div class="card-body"> 
            <h2 class="bi bi-tag-fill">カテゴリ作成</h2>
            <% if @label.errors.any? %>
              <div id="error_explanation">
                <h3><%= pluralize(@label.errors.count, "件の") %> エラーがあります</h3>
                  <% @label.errors.full_messages.each do |message| %>
                    <%= message %>
                  <% end %>
              </div>
            <% end %>
            <div class="form-group">
              <%= form.label "カテゴリ名" %>
              <%= form.text_field :name, class: "form-control", placeholder: "肉類" %>
            </div>
            <br>
            <div class="actions">
              <%= form.submit "作成する", class: "btn btn-sm btn-info" %>
            </div>
          </div>
        </div>
      </div>
    </div>        
  </div>
  <% end %>
  <br>
<%= link_to "戻る", labels_path, id: "back", class: "bi bi-skip-backward-fill btn btn-sm btn-primary" %>